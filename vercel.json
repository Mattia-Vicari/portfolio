{
    "builds": [{
        "src": "Portfolio/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Portfolio/wsgi.py"
        }
    ]
}